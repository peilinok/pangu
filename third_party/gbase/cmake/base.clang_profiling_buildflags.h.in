#ifndef BASE_CLANG_PROFILING_BUILDFLAGS_H_
#define BASE_CLANG_PROFILING_BUILDFLAGS_H_

#if defined(CLANG_PROFILING)
#define BUILDFLAG_INTERNAL_CLANG_PROFILING() (1)
#else
#define BUILDFLAG_INTERNAL_CLANG_PROFILING() (0)
#endif

#if defined(CLANG_PROFILING_INSIDE_SANDBOX)
#define BUILDFLAG_INTERNAL_CLANG_PROFILING_INSIDE_SANDBOX() (1)
#else
#define BUILDFLAG_INTERNAL_CLANG_PROFILING_INSIDE_SANDBOX() (0)
#endif

#if defined(USE_CLANG_COVERAGE)
#define BUILDFLAG_INTERNAL_USE_CLANG_COVERAGE() (1)
#else
#define BUILDFLAG_INTERNAL_USE_CLANG_COVERAGE() (0)
#endif

#endif  // BASE_CLANG_PROFILING_BUILDFLAGS_H_