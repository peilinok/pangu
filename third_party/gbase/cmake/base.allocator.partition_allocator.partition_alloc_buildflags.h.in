#ifndef BASE_ALLOCATOR_PARTITION_ALLOCATOR_PARTITION_ALLOC_BUILDFLAGS_H_
#define BASE_ALLOCATOR_PARTITION_ALLOCATOR_PARTITION_ALLOC_BUILDFLAGS_H_

#if defined(HAS_64_BIT_POINTERS)
#define BUILDFLAG_INTERNAL_HAS_64_BIT_POINTERS() (1)
#else
#define BUILDFLAG_INTERNAL_HAS_64_BIT_POINTERS() (0)
#endif

#if defined(USE_PARTITION_ALLOC)
#define BUILDFLAG_INTERNAL_USE_PARTITION_ALLOC() (1)
#else
#define BUILDFLAG_INTERNAL_USE_PARTITION_ALLOC() (0)
#endif

#if defined(USE_PARTITION_ALLOC_AS_MALLOC)
#define BUILDFLAG_INTERNAL_USE_PARTITION_ALLOC_AS_MALLOC() (1)
#else
#define BUILDFLAG_INTERNAL_USE_PARTITION_ALLOC_AS_MALLOC() (0)
#endif

#if defined(ENABLE_BACKUP_REF_PTR_SUPPORT)
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_SUPPORT() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_SUPPORT() (0)
#endif

#if defined(ENABLE_BACKUP_REF_PTR_SLOW_CHECKS)
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_SLOW_CHECKS() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_SLOW_CHECKS() (0)
#endif

#if defined(ENABLE_BACKUP_REF_PTR_FEATURE_FLAG)
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_FEATURE_FLAG() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_FEATURE_FLAG() (0)
#endif

#if defined(ENABLE_BACKUP_REF_PTR_FEATURE_FLAG)
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_FEATURE_FLAG() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_BACKUP_REF_PTR_FEATURE_FLAG() (0)
#endif

#if defined(ENABLE_DANGLING_RAW_PTR_CHECKS)
#define BUILDFLAG_INTERNAL_ENABLE_DANGLING_RAW_PTR_CHECKS() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_DANGLING_RAW_PTR_CHECKS() (0)
#endif

#if defined(ENABLE_DANGLING_RAW_PTR_FEATURE_FLAG)
#define BUILDFLAG_INTERNAL_ENABLE_DANGLING_RAW_PTR_FEATURE_FLAG() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_DANGLING_RAW_PTR_FEATURE_FLAG() (0)
#endif

#if defined(ENABLE_DANGLING_RAW_PTR_PERF_EXPERIMENT)
#define BUILDFLAG_INTERNAL_ENABLE_DANGLING_RAW_PTR_PERF_EXPERIMENT() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_DANGLING_RAW_PTR_PERF_EXPERIMENT() (0)
#endif

#if defined(ENABLE_POINTER_SUBTRACTION_CHECK)
#define BUILDFLAG_INTERNAL_ENABLE_POINTER_SUBTRACTION_CHECK() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_POINTER_SUBTRACTION_CHECK() (0)
#endif

#if defined(BACKUP_REF_PTR_POISON_OOB_PTR)
#define BUILDFLAG_INTERNAL_BACKUP_REF_PTR_POISON_OOB_PTR() (1)
#else
#define BUILDFLAG_INTERNAL_BACKUP_REF_PTR_POISON_OOB_PTR() (0)
#endif

#if defined(PUT_REF_COUNT_IN_PREVIOUS_SLOT)
#define BUILDFLAG_INTERNAL_PUT_REF_COUNT_IN_PREVIOUS_SLOT() (1)
#else
#define BUILDFLAG_INTERNAL_PUT_REF_COUNT_IN_PREVIOUS_SLOT() (0)
#endif

#if defined(USE_ASAN_BACKUP_REF_PTR)
#define BUILDFLAG_INTERNAL_USE_ASAN_BACKUP_REF_PTR() (1)
#else
#define BUILDFLAG_INTERNAL_USE_ASAN_BACKUP_REF_PTR() (0)
#endif

#if defined(USE_ASAN_UNOWNED_PTR)
#define BUILDFLAG_INTERNAL_USE_ASAN_UNOWNED_PTR() (1)
#else
#define BUILDFLAG_INTERNAL_USE_ASAN_UNOWNED_PTR() (0)
#endif

#if defined(USE_HOOKABLE_RAW_PTR)
#define BUILDFLAG_INTERNAL_USE_HOOKABLE_RAW_PTR() (1)
#else
#define BUILDFLAG_INTERNAL_USE_HOOKABLE_RAW_PTR() (0)
#endif

#if defined(ENABLE_GWP_ASAN_SUPPORT)
#define BUILDFLAG_INTERNAL_ENABLE_GWP_ASAN_SUPPORT() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_GWP_ASAN_SUPPORT() (0)
#endif

#if defined(FORCIBLY_ENABLE_BACKUP_REF_PTR_IN_ALL_PROCESSES)
#define BUILDFLAG_INTERNAL_FORCIBLY_ENABLE_BACKUP_REF_PTR_IN_ALL_PROCESSES() (1)
#else
#define BUILDFLAG_INTERNAL_FORCIBLY_ENABLE_BACKUP_REF_PTR_IN_ALL_PROCESSES() (0)
#endif

#if defined(FORCE_ENABLE_RAW_PTR_EXCLUSION)
#define BUILDFLAG_INTERNAL_FORCE_ENABLE_RAW_PTR_EXCLUSION() (1)
#else
#define BUILDFLAG_INTERNAL_FORCE_ENABLE_RAW_PTR_EXCLUSION() (0)
#endif

#if defined(RECORD_ALLOC_INFO)
#define BUILDFLAG_INTERNAL_RECORD_ALLOC_INFO() (1)
#else
#define BUILDFLAG_INTERNAL_RECORD_ALLOC_INFO() (0)
#endif

#if defined(USE_FREESLOT_BITMAP)
#define BUILDFLAG_INTERNAL_USE_FREESLOT_BITMAP() (1)
#else
#define BUILDFLAG_INTERNAL_USE_FREESLOT_BITMAP() (0)
#endif

#if defined(GLUE_CORE_POOLS)
#define BUILDFLAG_INTERNAL_GLUE_CORE_POOLS() (1)
#else
#define BUILDFLAG_INTERNAL_GLUE_CORE_POOLS() (0)
#endif

#if defined(ENABLE_POINTER_COMPRESSION)
#define BUILDFLAG_INTERNAL_ENABLE_POINTER_COMPRESSION() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_POINTER_COMPRESSION() (0)
#endif

#if defined(ENABLE_SHADOW_METADATA_FOR_64_BITS_POINTERS)
#define BUILDFLAG_INTERNAL_ENABLE_SHADOW_METADATA_FOR_64_BITS_POINTERS() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_SHADOW_METADATA_FOR_64_BITS_POINTERS() (0)
#endif

#if defined(USE_STARSCAN)
#define BUILDFLAG_INTERNAL_USE_STARSCAN() (1)
#else
#define BUILDFLAG_INTERNAL_USE_STARSCAN() (0)
#endif

#if defined(PCSCAN_STACK_SUPPORTED)
#define BUILDFLAG_INTERNAL_PCSCAN_STACK_SUPPORTED() (1)
#else
#define BUILDFLAG_INTERNAL_PCSCAN_STACK_SUPPORTED() (0)
#endif

#if defined(ENABLE_PKEYS)
#define BUILDFLAG_INTERNAL_ENABLE_PKEYS() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_PKEYS() (0)
#endif

#if defined(ENABLE_THREAD_ISOLATION)
#define BUILDFLAG_INTERNAL_ENABLE_THREAD_ISOLATION() (1)
#else
#define BUILDFLAG_INTERNAL_ENABLE_THREAD_ISOLATION() (0)
#endif


#endif  // BASE_ALLOCATOR_PARTITION_ALLOCATOR_PARTITION_ALLOC_BUILDFLAGS_H_